# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test coverage output (npm run test:coverage)
coverage

# The static board mirror (README §2). The Pages deploy fetches it from the
# backend into the artifact, so it exists at build time and never in git —
# six commits a day of derived prices would bury the actual history.
public/data/
